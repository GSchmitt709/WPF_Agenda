<Window x:Class="Agenda.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Agenda"
        mc:Ignorable="d"
        Title="Agenda LaManu" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_Clients">
                    <MenuItem Header="_Ajouter" Click="AddCustomer_Click" />
                    <MenuItem Header="_Liste" Click="CustomersList_Click" />
                </MenuItem>
                <MenuItem Header="_Courtiers">
                    <MenuItem Header="_Ajouter" Click="AddBroker_Click" />
                    <MenuItem Header="_Liste" Click="BrokersList_Click" />
                </MenuItem>
                <MenuItem Header="_RDV">
                    <MenuItem Header="_Ajouter" Click="AddAppointment_Click" />
                    <MenuItem Header="_Liste" Click="AppointmentsList_Click" />
                </MenuItem>

            </Menu>
        </DockPanel>
        <Frame x:Name="frame" Content="" Margin="10,30,10,10"/>
    </Grid>
</Window>
